<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_id %><br />
    <%= f.number_field :photo_id %>
  </div>
  <div class="field">
    <%= f.label :tag_value %><br />
    <%= f.text_field :tag_value %>
  </div>
  <div class="field">
    <%= f.label :xpos %><br />
    <%= f.number_field :xpos %>
  </div>
  <div class="field">
    <%= f.label :ypos %><br />
    <%= f.number_field :ypos %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
